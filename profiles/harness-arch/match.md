# Harness 架构

当一条内容是新的 agent harness、coding agent 运行时、或现有 harness 的架构级变化时，使用此配置。

包括：Claude Code / Codex / OpenHands / Goose / Cline / Roo / Aider / Continue / SWE-agent 的 Release 与设计文章；MCP 与工具接口；权限、沙箱、记忆、规划、子代理、评测 harness；可复现的架构分享。

不包括：与 harness 无关的通用 LLM 新闻、应用层提示词、营销发布会、没有技术细节的「又一个 agent 产品」。
